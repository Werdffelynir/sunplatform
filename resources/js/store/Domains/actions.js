// import SomeService from '@/services/some.service'

export const ACTION_GET_USER = 'GET_ITEM';
export const ACTION_LOAD_DOMAINS = 'ACTION_LOAD_DOMAINS';

export default {

    [ACTION_GET_USER] ({ commit }) {
        // some request and commit mutations
    },

    [ACTION_LOAD_DOMAINS] ({ commit }) {
        // some request and commit mutations
    },

}
